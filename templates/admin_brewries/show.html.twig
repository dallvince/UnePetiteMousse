{% extends 'base.html.twig' %}

{% block title %}Gestion des brasseries | {{ parent() }} {% endblock %}

{% block h1 %}Gestion des brasseries{% endblock %}

{% block body %}
    <h1>Brasseries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brewry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brewry.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ brewry.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_brewries_index') }}">back to list</a>

    <a href="{{ path('app_admin_brewries_edit', {'id': brewry.id}) }}">edit</a>

    {{ include('admin_brewries/_delete_form.html.twig') }}
{% endblock %}
