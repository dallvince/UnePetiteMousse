{% extends 'base.html.twig' %}

{% block title %}Styles{% endblock %}

{% block body %}
    <h1>Styles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ style.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ style.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ style.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ style.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_styles_index') }}">back to list</a>

    <a href="{{ path('app_admin_styles_edit', {'id': style.id}) }}">edit</a>

    {{ include('admin_styles/_delete_form.html.twig') }}
{% endblock %}
