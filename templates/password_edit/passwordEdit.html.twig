{% extends 'base.html.twig' %}

{% block title %}Modifier mon Mot de passe | {{ parent() }}{% endblock %}

{% block body %}

    <a class="btn btn-secondary ms-3" href="{{ path('app_profil') }}">Retour</a>

    <div class="col-md-8 m-auto">
        {{ form_start(formPasswordEdit)}}
            {{ form_row(formPasswordEdit.plainPassword)}}

            <div class="d-flex justify-content-center">
                <input type="submit" value="Modifier" class="btn btn-warning col-md-2 mt-3">
            </div>

        {{ form_end(formPasswordEdit)}}


    </div>

    <div class="d-flex justify-content-center">
    </div>

    

{% endblock %}